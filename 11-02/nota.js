let nota = prompt("digite sua nota");


if (nota >= 6) {
    document.write(`sua nota é ${nota} e vc esta aprovado`);
} else if (nota <= 5) {
    document.write(`sua nota é  ${nota} e vc esta reprovado`);
}